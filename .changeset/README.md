# Changesets

Run `pnpm changeset` after making a change that should ship in `@m3000/market`.

The generated markdown files in this folder tell Changesets how to bump package versions
and what to include in the release notes.
